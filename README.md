# Joker-XD CORE (pairing locked)

This is the **CORE** build.

- Link-device / pairing code generation is **disabled**
- Public `/pair` endpoint returns 403
- Only existing sessions (auth already on disk) can restore
- Use the **official loader** to pair new numbers

Do not expect this package alone to generate pairing codes.
